import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '006-app-intents',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
